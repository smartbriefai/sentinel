[pytest]
# Enable asyncio mode for all async test functions.
# Required by pytest-asyncio >= 0.21 (and 1.x).
asyncio_mode = auto

# Treat the project root as the base for test discovery
testpaths = tests

# Show short tracebacks for readability; -s lets print() appear in output
addopts = --tb=short
